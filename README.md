# 宇宙ビジネス動向レポート自動生成システム

## 概要
本プロジェクトは、世界の宇宙関連ビジネス動向に関するニュースを収集・分析し、毎週月曜日の朝9時に30枚のスライド資料として自動生成し、Webサイト（HP）に掲載するシステムです。

## Claude Codeへの指示（重要）
あなたは本システムの開発・構築を担うAIエージェント（Claude Code）です。
同封されている `implementation_plan.md`（実装計画）と `tasks.md`（タスク一覧）に加え、**必ず `plan/` ディレクトリ配下にある詳細実装計画書（`01_overview.md` ～ `05_deployment.md`）をすべて読み**、それに従ってコードの記述と実行を行ってください。

### 開発・運用における絶対のルール
1. **セキュリティの徹底**: 
   - 提供された Gemini APIキー（`AIzaSyAjSWtRq2LcwlVu4-eGvVMrZOQevblZJi0`）やその他クレデンシャル情報は、絶対にコード内にハードコードしないでください。
   - 必ず `.env` ファイルを使用し、`.gitignore` で `.env` をバージョン管理から除外して漏洩を防いでください。
2. **変更の承認プロセス**: 
   - システムの根幹（主要な構成、データ処理ロジック、外部連携など）を変更・修正・削除する場合は、**必ず事前にユーザーへ承認を求めてください**(Ask for user approval)。
   - 承認を求める際は、「どのような変更・削除を行うのか」「それによってどのような影響があるのか」を**平易な言葉で説明**した上で承認作業に進んでください。
